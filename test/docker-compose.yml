# This docker-compose file runs an empty ldap server 
version: '2'
services:
  test:
    image: 'petzi/openldap-test'
    build: .
    environment:
      SLAPD_ROOTDN: "dc=example_xyz,dc=org"
      SLAPD_ROOTPW: "NotSoSimpleYetNotHard"
      FIRST_USER: "myfirsttestuser"
      FIRST_USER_PASSWORD: "myftu-password"
    ports:
      - "2389:389"
